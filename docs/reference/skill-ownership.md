# Skill Ownership

The canonical source for `agents-md-editor` is `skills/agents-md-editor/` in this repository.

Update the repository first, then sync the installed skill copy from `perhapsspy/agents-md-editor` so install metadata and the runtime copy stay traceable.
